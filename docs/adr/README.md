# Architecture Decision Records

This directory preserves the architectural decisions behind the Notidian fork.

The governing rule is that **Obsidian vault data remains canonical**. Notidian may provide richer database views, but ordinary file identity and note metadata must not become governed by a hidden context database.

For implementation status, read [Current State](../current-state.md). For the full current architecture, read [Notidian System Architecture](../notidian-system-architecture.md). For practical table behavior, read [Table Database Workflows](../table-database-workflows.md).

## Active Records

These records define the current Notidian architecture and implemented safety model.

| ADR | Decision | Purpose |
| --- | --- | --- |
| [0001](0001-authority-partitioned-database-model.md) | Authority-partitioned database model | Defines ownership for file identity, frontmatter properties, context-native fields, view state, and computed values. |
| [0002](0002-frontmatter-backed-context-columns.md) | Frontmatter-backed context columns | Explains how YAML properties become visible/editable table columns without making MDB rows durable metadata. |
| [0003](0003-editable-page-titles-through-file-renames.md) | Editable page titles through file renames | Canonical full record for why page-title edits must be file rename transactions. |
| [0006](0006-unified-table-edit-transactions.md) | Unified table edit transactions | Defines the shared execution path for value edits, field edits, paste writes, and future grid gestures. |
| [0007](0007-table-edit-feedback.md) | Table edit feedback | Defines transient pending, failed, skipped, and conflict cell feedback. |
| [0008](0008-table-undo-journal.md) | Table undo journal | Defines table-local undo/redo and why replay goes through authority-aware write paths. |
| [0009](0009-frontmatter-conflict-detection.md) | Frontmatter conflict detection | Defines stale-frontmatter detection so table edits do not overwrite newer canonical metadata. |
| [0010](0010-legacy-context-audit-and-migration.md) | Legacy context audit and migration | Defines audit-first migration so legacy context values are not lost. |
| [0014](0014-notidian-only-personal-database-engine.md) | Notidian-only personal database engine | Current governing strategy: Notidian is the only intended database engine/interface. |
| [0015](0015-canonical-schema-planning.md) | Canonical schema planning | Defines frontmatter property discovery, create, rename, and delete previews before destructive schema UI/apply flows. |
| [0017](0017-explicit-notidian-ownership.md) | Explicit Notidian ownership | Closes the silent authority fallback: durable MDB ownership requires an explicit `source: "notidian"` marker, and source-less file-backed columns default to frontmatter. Refines ADR 0001. |
| [0018](0018-makemd-fork-debt-scope-and-frame-trust-boundary.md) | Make.md fork-debt scope + frame trust boundary | Scopes the Make.md-era subsystems. Landed: MKit installer disabled-by-default + `spaceSubFolder` locked. Frames kept (load-bearing); sink hardening tracked for live-verified work (bd Notidian-vke, -ala). |
| [0019](0019-select-to-comment-anchoring-and-ai-review-channel.md) | Select-to-comment anchor + AI-review channel (**Proposed**) | Open product/UX decision: how a comment binds to a selection (recommends Obsidian `^block` id) and the AI-directed review comment format the Atlasidian parser consumes (recommends frontmatter `review.comments`). Awaiting owner direction (bd Notidian-o4w). |
| [0020](0020-date-reminders-and-recurring-events.md) | Date reminders + recurring events (**Proposed**) | Open design decision: reminder delivery (recommends a default-OFF load-time/interval scan firing `superstate.ui.notify`, no external plugin) and recurrence materialization (recommends single-row rrule-shaped `repeat` frontmatter expanded at render time, never generated rows — generalizing the existing calendar expander). Awaiting owner direction (bd Notidian-5io). |
| [0022](0022-frame-execution-settings-toggle-and-trusted-frame-allowlist.md) | Frame-execution settings toggle + trusted-frame allowlist (**Proposed**) | Open design decision following ADR 0018 / bd Notidian-vke: (1) settings-UI toggle for `hardenFrameExecution` (recommends an `advanced`-category, tradeoff-naming toggle); (2) a vault-trusted-frame allowlist that does **not** reopen the RCE — recommends a non-persisted, user-blessed, session-scoped provenance stamp (optionally a content-hash allowlist), never a persisted/attacker-editable "trusted" flag (preserves the `trust.ts` invariant). Gated on the owner first keeping `hardenFrameExecution` ON after live-verify. Awaiting owner direction (bd Notidian-214). |
| [0024](0024-sub-items-back-relations-ux.md) | Sub-items + back-relations UX: property authority, creation, cycles, sort (**Proposed**) | Open product/UX decision for epic item (3); engine/render already shipped (gg9/pv4/s9m/ahk). Load-bearing call: is sub-item creation **one-way (child owns the parent link)** or two-way? Recommends **one-way** — the inverse already exists as read-only computed back-relations, so two-way only duplicates a derivable fact and adds a second authority + reconciliation. Also recommends per-view parent-column designation (status quo), a passive cycle indicator with no edit-time block, and keeping the shipped sort/filter/groupBy rules. Awaiting owner direction (bd Notidian-2uz). |
| [0025](0025-array-comparator-correctness.md) | array.ts order comparators — correctness vs caller-dependence (+ uniqCaseInsensitive casing) (**Proposed**) | Open behavior decision: the two order comparators (`orderStringArrayByArray`/`orderArrayByArrayWithKey`, column + space ordering) are non-reflexive/non-transitive (`cmp(x,x)===-1`), rely on V8 TimSort specifics for absent-item order, and mutate the caller's array in place; current output is **locked as characterization** in `array.test.ts`. Recommends **Option B** — replace with a stable, reflexive, non-mutating comparator and flip the locked assertions — over (A) keep+document or (C) flag-gate, because the present-first invariant callers depend on is already property-tested and neither caller depends on the reversed-absent quirk (one is actively harmed by it — duplicated paths in `cacheParsers.ts:88`). Folds in **Notidian-9v6**: switch `uniqCaseInsensitive` to first-seen casing (display-only). Awaiting owner direction (bd Notidian-e8e). |
| [0026](0026-jsonwithunquoted-frame-payload-parsing.md) | `jsonWithUnquoted` frame-payload parsing — wrapper convention + tolerant tokenizer (**Proposed**) | Open trust-boundary decision following ADR 0018 / bd Notidian-vke: `jsonWithUnquoted.ts` is the boundary between stored frame-action text and code compiled by `runner.ts` `new Function`. (1) Wrapper-convention asymmetry — `"{...}"` parses to a STRING (fast-path), `'{...}'` to an OBJECT; recommends making the **OBJECT convention canonical** (every caller wants the object). (2) The lossy regex parser silently degrades to `{}` on embedded `,`/`}`/`]`; recommends a **tolerant tokenizer gated behind the existing default-OFF `hardenFrameExecution` flag** (a more permissive parser widens the executable-payload set, so it must ride the vke flag + live-verify) — **no new runtime flag**. Parser code untouched. Awaiting owner direction (bd Notidian-fs6). |
| [0027](0027-bd-embedded-dolt-export-stats-blind-and-jsonl-mirror-policy.md) | Upstream bd embedded-dolt `export`/`stats` blind to issues — JSONL mirror policy (**Proposed**) | Owner/upstream action item, **no in-repo fix exists**: on bd 1.0.5 (embedded-dolt) `bd list --all` sees all 113 issues but `bd stats`/`bd export` read the committed `issues` table, which is empty at every Dolt root, so the passive `.beads/issues.jsonl` mirror stays empty (daily bd work is unaffected). Recommends **(1a)** file upstream (`steveyegge/beads` or fork `gastownhall/beads`) with the minimal repro and retest `bd export` per release, closing Notidian-osf when a release re-exports the full graph; and **(2a)** keep the JSONL mirror **empty by design** (never fabricate a lossy hand-built mirror from `bd list --json`). Awaiting owner direction (bd Notidian-nir). |
| [0029](0029-frontmatter-relations-rollups-authority-ux.md) | Frontmatter-link relations + rollups: authority + UX contract — source property, rollup-definition storage, one-way vs two-way, dangling/non-numeric display, recompute timing (**Proposed**) | Open product/authority decision for epic item (1) — the headline Notion gap; engine/runtime/cell/column-type/config-menu already shipped (9ln/8pl/e1u/ahk). Load-bearing calls: **(b)** the rollup definition `{ref, field, fn}` lives in MDB **view/column config**, which ADR 0001/0014 authorize — it is configuration, not a durable frontmatter value, so it does **not** trip ADR 0017 (the result is always recomputed, the links stay in frontmatter); **(c)** relations stay **one-way** (source owns the link; the inverse is the read-only computed `backlink`), symmetric with ADR 0024 B1 — two-way only as a future per-DB opt-in. Also recommends any link column as the relation source per rollup/view (status quo, no reserved name), a passive "N of M counted / K unresolved" indicator over silent partial numbers, and live on-render recompute off the in-memory `pathsIndex` (a rollup is computed, never stored). Awaiting owner direction (bd Notidian-tni). |
| [0028](0028-per-database-row-create-templates.md) | Per-database row-create templates: where they live, what they seed, how chosen, schema-default interaction (**Proposed**) | Open product decision for epic item (2); storage already shipped — templates are file-canonical `.md` notes copied whole (frontmatter+body) via `newTemplateInSpace`, and the navigator `+` already applies a default + offers a per-DB picker. Gap: the three in-table/context row-create chokepoints (`TableView.tsx newRow`, `api.ts insert`, `ContextCell.tsx`) bypass templates and create empty files. Recommends keeping templates **file-canonical `.md`** (never an MDB blob without `source:notidian` — ADR 0017; the MDB holds only the default *pointer* = view config), seeding **frontmatter + body** (frontmatter-only is Type Profile's job), **auto-applying the single default + keeping the existing optional picker** (no forced prompt), and **template-wins-wholesale** over Type Profile `newPropertyDefaults` (the shipped precedent — one writer per row create). Optional default-OFF wiring spike offered, not built. Awaiting owner direction (bd Notidian-e29). |
| [0030](0030-multi-display-string-comma-escaping.md) | `serializeMultiDisplayString`/`parseMultiDisplayString` — first-comma-only escape data loss (**Proposed**) | Open behavior/data-migration decision: both serializer + parser use a string-pattern `.replace(',', ...)` (first-occurrence only), so any element containing a comma fractures on round-trip (`['a,b','c']` -> `['a','b','c']`; verified empirically). Backs link/context cells, tags, aliases, lookup links/spaces; current output is **locked as characterization** in `serializers.test.ts` (Notidian-a3s). Exposure is narrow — the `multi` path already uses safe JSON `serializeMultiString`; paths/tags are comma-safe in practice, so the real bite is a comma in an alias/single-option label. Recommends **Option A** — make both replaces global (`/,/g`, `/\,/g`) and move the parser un-escape to *after* the split, keeping the human-readable display form and flipping the locked assertions — over **B** (JSON-encode single values, a readability regression) or **C** (accept + document a confirmed data-loss defect). Load-bearing catch: fixing the parser re-interprets values already written by the buggy serializer (one eyes-on vault confirm settles it). Awaiting owner direction (bd Notidian-od7). |
| [0031](0031-csv-import-duplicate-header-contract.md) | `parseCsvToRecords` — duplicate CSV headers silently clobber (last-write-wins) on import (**Proposed**) | Open behavior decision on the Notion-parity CSV import path: name-keyed `record[h]=cells[i]` collapses two same-named columns (later wins, first column's data silently dropped) while `headers[]` still lists the duplicate. Recommends **Option B** — auto-uniquify duplicate headers in the **parser** via `uniqueNameFromString` (`a,a,b` -> `['a','a1','b']`), the only layer that sees the raw header row positionally; lossless, matches Notion, and the column-mapping preview shows the suffixed name before any write. Over **A** (reject/warn — a hard failure on mechanically-repairable input) or **C** (keep silent clobber). Awaiting owner direction (bd Notidian-5zc). |
| [0032](0032-date-filter-boundary-and-invalid-date-semantics.md) | Date-filter boundary + Invalid-Date semantics (`dateAfter`/`dateBefore`/`isSameDay`) (**Proposed**) | Open behavior/UX decision on the table-view row-visibility path: `dateAfter` uses inclusive `>=` (line 97) but `dateBefore` uses exclusive `<` (line 106), so a boundary instant satisfies one and not the other; because comparison is at **instant** granularity while users pick a **date**, "on the boundary day" splits by the row's stored time-of-day. An unparseable value becomes Invalid Date (NaN), invisible to **both** filters (fail-closed). `isSameDay` compares only month+day, **ignoring the year**. Current output **pinned as characterization** in `filter.test.ts` (Notidian-3fs). Recommends **(a)** make `dateAfter`/`dateBefore` **day-granular + both inclusive** (A1) so the boundary day matches both consistently; **(b)** keep Invalid-Date **invisible-to-both / fail-closed** (B1) — a malformed date must not silently satisfy a date filter; **(c)** fix `isSameDay` to also compare the **year** (C1; `isSameDayAsToday` stays year-agnostic by design). Notes Notidian-37m (`filterReturnForCol` fail-open) as an adjacent predicate-contract decision the owner may fold in. Awaiting owner direction (bd Notidian-qbr). |
| [0033](0033-intelligentcompare-viz-comparator-non-transitivity.md) | `intelligentCompare` (Visualization `sortingUtils.ts`) — non-transitivity vs observable chart ordering (**Proposed**) | The `Notidian-e8e`/ADR-0025 comparator bug class on the chart-ordering surface: `intelligentCompare` is reflexive + antisymmetric but **NON-TRANSITIVE** because the date/number/string branch is chosen **per-pair** (`isDateLike(aStr)||isDateLike(bStr)`), not per-value, so a value (e.g. `"10"`) is classified as a number against `""` but as a Date (`year 2001`) against a real date — giving `cmp("2024-01-01","")=-1`, `cmp("","10")=-1`, `cmp("2024-01-01","10")=+1` (verified; 295 violations over an 18-value domain). It is fed directly to `Array.prototype.sort` for chart axis/category order (`D3VisualizationEngine.tsx:205,388`; `LineChartUtility.ts`; Bar/Line/Area/Radar transformers), so the rendered order is a V8/TimSort artifact for mixed-type data. Current behavior **locked as characterization** (`KNOWN DEFECT` blocks) in `sortingUtils.test.ts` (Notidian-dx5); self-consistent sub-domains provably obey the full triad. Recommends **Option B** — classify each value's type **once** (per-value buckets, fixed cross-bucket order) into a real strict weak ordering, then flip the locked assertions — over **A** (keep+document the latent hazard) or **C** (flag-gate pure offline-provable logic). Notes `Notidian-dox` (getOptionsOrder throw + falsy/`0` value drops) as adjacent robustness gaps. Awaiting owner direction (bd Notidian-0id). |
| [0034](0034-predicate-unknown-fn-fail-open-contract.md) | `filterReturnForCol` fail-open contract for unknown/undefined filter `fn` (**Proposed**) | Open behavior/safety decision on the table-view row-visibility path: `filterReturnForCol` initializes `result = true` (filter.ts:144) and only overrides it when `filterFnTypes[filter?.fn]` resolves a **known** operator, so a `null` filter, a missing `fn`, or an unknown `fn` (e.g. from a newer schema version) makes the row **stay visible** — a corrupt/unrecognized predicate silently disables filtering rather than hiding rows or throwing. Pinned as characterization in `filter.test.ts` (Notidian-3fs, lines 526/532/591); every call site already fails open (`linkContextRow.ts:372/:378`, `ContextEditorContext.tsx:681`, `treeHelpers.ts:214`), and `validatePredicate`→`cleanPredicateType` (predicate.tsx:38) **already strips** unknown fns at write/load, so fail-open here is the defensive backstop. Recommends **Option A** — keep fail-open + document the contract, paired with a **one-time upstream validate-loud warning** in `cleanPredicateType`/`validatePredicate` — over **B** (fail-closed: hides the owner's own data with no signal — the worse failure for a single-user vault) or **C** (throw/log per-row: render-crash/console-spam on the hot path). Same family as ADR 0032(b) but **defensibly resolves oppositely** (value-level malformed date = fail-closed; operator-level unreadable constraint = fail-open). Awaiting owner direction (bd Notidian-37m). |
| [0035](0035-inferencodingtype-numeric-vs-temporal-value-inference.md) | `inferEncodingType` value-path — numeric data infers as `temporal`, shadowing `quantitative` (**Proposed**) | Open heuristic-quality decision on the chart-encoding type path (ADR 0033 bug family): the no-property-metadata value path runs `areDates` **before** `areNumbers`, and `new Date(String(n))` is **valid** for stringified real numbers and bare numeric strings (verified: `String(1)`, `String(2.5)`, `"2024"` all parse to valid Dates), so arrays of genuine numbers / numeric strings infer **`temporal`**, shadowing `quantitative` — the only value-path to `quantitative` is non-date-parseable numbers (booleans). Blast radius is metadata-less inference only; explicit `number` property metadata still yields `quantitative`. Fed via `ensureCorrectEncodingType` into D3 axis/scale typing across every transformer. Current numeric→temporal output **locked as characterization** in `inferEncodingType.test.ts` (Notidian-5hs ADVERSARIAL block). Recommends **Option C/A hybrid** — in the value path treat a value as a date candidate **only when `Number(String(v))` is `NaN`** (or `v` is a `Date`), so numbers prefer `quantitative` while genuine ISO/date-string and `Date` detection are preserved — over **A** (bare reorder / heavyweight date-regex) or **B** (leave as-is, rely on metadata, documents a foot-gun). Names the unavoidable years-as-numbers ambiguity (C types year-like numbers quantitative; intent goes via metadata/explicit type). Any chosen change must flip the locked numeric `temporal` assertions in the same commit. Awaiting owner direction (bd Notidian-sp5). |
| [0037](0037-datapipeline-normalizeconfig-purity-and-validate-guard.md) | `DataTransformationPipeline.normalizeConfig` impurity (mutates caller `config.encoding`) + `validateConfig` throws on undefined `encoding` (**Proposed**) | Open purity/robustness decision on the Visualization orchestrator (ADR 0033/0035 subtree): (1) `normalizeConfig` (`:34`) shallow-copies (`{ ...config }`), so it **mutates the caller's `config.encoding` in place** when writing inferred encoding types — locked as characterization (`out.encoding === original.encoding`, test 211-227/419-424). (2) `validateConfig` (`:265`) dereferences `config.encoding.x` un-guarded, so it **throws** on `encoding===undefined` (locked 807-815) — but has **ZERO production callers**, so the throw is unreachable today. **Decisive finding:** the mutation is **load-bearing in `D3VisualizationEngine`** — the `scales` memo (`:133-456`) reads `config.encoding.*.type` directly and re-derives it only for X(scatter/line/bar/area) + Y(scatter); for Y(bar/line/area/pie/radar) and X(pie/radar) the type comes **only** from the side-effect. Verified empirically: a deep-clone in `normalizeConfig` with no other change leaves those types `undefined` and the chart **loses its Y axis** — exactly the bead's flagged downstream-identity risk, confirmed. Recommends **Option A** — clone the `encoding` subtree to make `normalizeConfig` **pure** **and** make the engine self-sufficient (re-derive Y for all chart types + X for pie/radar) **in the same change**, plus add the `validateConfig` early-guard; flip the three locked assertions + one eyes-on chart check — over **B** (leave as-is, document the load-bearing mutation + the throw) or **C1** (guard `validateConfig` only — free, render-safe, no eyes-on). Ruled out: **C2** clone-only (empirically breaks the render path; the bead's "clone but don't guard" is not a safe partial) and a blind autonomous fix (flips a locked identity assertion the render path depends on). Awaiting owner direction (bd Notidian-jko). |
| [0038](0038-areachart-missing-x-encoding-contract.md) | `AreaChartTransformer` throws on a missing x encoding — alone among the six transformers (**Proposed**) | Open robustness/uniformity decision on the Visualization data path (ADR 0033/0035/0037 subtree): `AreaChartTransformer.transform` **THROWS** `TypeError: Cannot read properties of undefined (reading 'type')` for a missing-x encoding (`undefined` or `[]`) on non-empty data, because the main-body temporal-fill block dereferences `xEncodings[0].type` **un-guarded** at `AreaChartTransformer.ts:154`, while its own sibling check at `:108` is guarded (`xEncodings[0]?.type`) and all three per-branch helpers guard correctly. All **five** other transformers return the safe empty contract for a missing-x encoding — Bar (`:119` early-return), **Line (its architectural twin — wraps the same fill block in `if (xEncodings[0])` at `:86`)**, Radar (`:51`), Pie (`:27`), Scatter (default-field fallback); Area alone diverges to a throw. The throw is **caught** by `DataTransformationPipeline`'s try/catch (`:127-181`) and surfaced as an **error-state render** (`data:null`+error) instead of an empty chart; reachable because `normalizeConfig` does not synthesize a missing x, so a half-configured area chart (no X field picked yet) hits it. Current throw **LOCKED as `KNOWN DEFECT`** characterization in `AreaChartTransformer.test.ts` (Notidian-kxq). Recommends **Option A** — add the sibling-style early-return of the empty `AreaChartData` contract when `!xEncodings[0]?.field` (mirroring the twin Line), then flip the two locked `toThrow` assertions to expect the empty contract — over **B** (make all five siblings throw too — large blast radius, inverts five working contracts to match one defect) or **C** (keep + document the divergence). Ruled out: blind autonomous adoption of A (owner-visible render delta + the characterization deliberately pins the throw) and a default-OFF flag (a 1-line guard on pure offline-provable logic with no security/authority sink — nothing a flag de-risks). Awaiting owner direction (bd Notidian-drp). |
| [0036](0036-stripfrontmatterfromstring-greedy-regex-dead-helper.md) | `stripFrontmatterFromString` greedy/unanchored regex — fix, replace, or delete a dead helper (**Proposed**) | Open scoping decision: `stripFrontmatterFromString` (`fm.ts:30`) uses `/---(.|\n)*---/` — greedy + unanchored — so a real leading frontmatter block plus a body horizontal-rule `---` over-strips the intervening prose (`"---\ntitle: Hi\n---\nIntro\n\n---\n\nAfter"` -> `"\n\nAfter"`, `Intro` LOST) and it matches non-leading fences. Behavior **pinned as characterization** in `fm.stripFrontmatterFromString.test.ts` (Notidian-bey). **Decisive audit finding: the helper has ZERO production callers** — the only historical caller (a commented-out preview reader over `vault.cachedRead`, commit `58bc881`) was deleted in `b38b417`. The repo already has **two correct, in-use frontmatter strippers**: `stripFrontmatter` (`spaceNoteBody.ts:4`, anchored/lazy/CRLF-safe) and the Obsidian `frontmatterPosition` offset slice (`markdownAdapter.ts:335`, `Explorer.tsx:322`). So the real question is not "what should it strip" but "should this defective duplicate exist." Recommends **Option C** — **delete** the dead helper + its characterization test (removes the over-strip hazard, cannot regress since nothing observes its output, collapses three strippers to the two correct ones) — over **A** (fix in place to an anchored/lazy regex; choose only if an out-of-tree importer must keep the export) or leaving it as a copy-paste trap. No flag/eyes-on needed (the function never runs on a note body in the current codebase). Awaiting owner direction (bd Notidian-2zs). |
| [0040](0040-emojibox-dead-injection-sink.md) | `emojiBox.ts` dead, unsanitized SVG/HTML injection sink — delete vs sanitize-and-keep (**Proposed**) | Security-flavoured sibling of ADR 0036: `src/shared/utils/emojiBox.ts` (`createExactEmojiBox`/`emojiBoxStyles`/`calculateOptimalEmojiFontSize`/`supportsColorEmoji`) has **zero production callers** (grep over `*.ts`/`*.tsx` finds only the file's own definitions; nothing imports the module), and `createExactEmojiBox` returns raw SVG/`<span>` HTML built by interpolating caller input with **no escaping** — `${emoji}` as element text, `${size}` into `style`, `class="${options?.className}"` — a latent `innerHTML`/`dangerouslySetInnerHTML` XSS sink under the ADR 0017 / Notidian-ebz threat model (it even constructs the `<foreignObject>` the icon sanitizer removes). **Decisive finding:** the codebase already renders emoji correctly through the sanitized house path at `StickerModal.tsx:26-33` — `escapeHtml(emojiFromString(...))` for emoji, `sanitizeIconSVG(...)` for custom SVG — built by the Notidian-ebz sweep, so `emojiBox` is a third, defective, unused injection path. Recommends **Option A** — **delete the whole module** (removes the attractive-nuisance sink, cannot regress since nothing observes its output, re-collapses emoji injection to the one hardened in-use path) — over **B** (keep but route `emoji`/`className`/`size` through `escapeHtml`/`sanitizeIconSVG` + a CSS-length guard, drop/replace the `foreignObject` method, add adversarial + property tests **before** any caller — choose only if an out-of-tree importer must keep the exports or a future exact-emoji-box feature is intended) or **C** (keep as-is — rejected, violates the sink invariant the moment anyone calls it). No flag/eyes-on needed (the module never renders in the current codebase; A is tsc/jest/build-provable, B is provable by function-boundary tests). Awaiting owner direction (bd Notidian-k6a5). |
| [0039](0039-control-byte-source-guard.md) | Control-byte source guard — prevent raw NUL/C0 controls re-entering tracked source (**Proposed**) | LOW-value regression-insurance decision (follow-up to Notidian-5qq; the repo is **verified clean** — a sweep of all tracked files for bytes `<0x20` outside tab/nl/cr hits only the **tracked, legitimately-binary** `main.js` esbuild bundle, which has C0 bytes `0x01`-`0x06` but no NUL). The *what* (no raw NUL/C0 control bytes in tracked source — a NUL binarizes a file so audits/grep silently skip it, the false-negative trap that derailed Notidian-2lg) is clear; the *where to hook + which mechanism* is open. Recommends **Option A** — a tiny ~30-line Node byte-scanner (`scripts/notidianControlByteScan.js`) wired as a step in the **existing** `npm run verify:source` harness (`notidianVerify.js`), with an **explicit allowlist** (`main.js` + binary assets) so the legitimately-binary bundle is not a false positive, plus an optional `.husky/pre-commit` shim — over **B** (escalate `no-control-regex` to error: JS/TS-and-regex-only, blind to NUL in strings/comments/fixtures/non-JS, and the **two existing local disables** at `sanitize.ts:347`/`sanitizePrimitives.property.dom.test.ts:139` prove it is fragile/bypassable for this) or **C** (`.gitattributes` text coercion: normalizes line endings only, does **not** detect/block control bytes). Decisive nuance: there is **no CI** (`.github/workflows/` absent) and **no pre-commit infra**, so A binds to the already-run verification gate rather than standing up new infrastructure. Ruled out: a naive allowlist-less scan (false-positives on `main.js`), a default-OFF runtime flag (build tooling, not a runtime surface), and a blind build (repo is clean — no defect, and the mechanism touches dev-workflow shape). Awaiting owner direction (bd Notidian-jlb5). |
| [0041](0041-consolidate-view-search-affordances.md) | Consolidate the view search affordances — filter-search vs Cmd/F quick-find (**Accepted**; keep-dormant sub-decision **superseded by [ADR 0049](0049-remove-dormant-quick-find.md)**) | Open product-shape decision (one vs two in-view search affordances) on a recently-shipped, **never-owner-verified** feature (quick-find, ADR 0021 / Notidian-r20). The table toolbar carries **two adjacent search-shaped controls**: the magnifier `SearchBar` (placeholder "Type to search...", **filters/hides** rows via `searchString`+`matchAny`, `ContextEditorContext.tsx:691-712`) and the `⌕` `QuickFindBar` ("Find in view", `Cmd/Ctrl+F`, **highlights+navigates**, never hides; `tableQuickFind.ts`, `TableView.tsx:854-863`). Owner reports the second is "redundant" and "doesn't work"; offline review **cannot confirm the defect** (render path). Notion parity = **one** in-view search (highlight-first) + structured Filter as its own surface. Recommends **Option A** — consolidate to one toolbar search, bind `Cmd/F` to it, remove the standalone `⌕` button; sub-variant **A1** (recommended): fold highlight in as a mode of the one search (keeps both engines + the no-`innerHTML`-sink cell-class highlight, reaches parity, loses no capability) over A2 (delete the engine, regresses parity) or C (make quick-find the only search — most parity-faithful but gated on the live repro passing). **Ruled out: B "keep both, just relabel"** — treats a product-shape problem as a copy problem and leaves the redundancy in place. Also records an **independent live-repro action**: confirm whether quick-find is genuinely broken in the owner's vault, regardless of the consolidation pick. View-layer only; no code changed. Awaiting owner direction (bd Notidian-z8q). |
| [0042](0042-nativetounified-empty-input-contract.md) | `nativeToUnified('')` empty-input contract — guard to a total codec vs. caller precondition (**Proposed**) | Open behavior decision on the emoji codec util `src/shared/utils/stickers.ts:30` — `nativeToUnified = (native) => native.codePointAt(0).toString(16)` throws `TypeError` on `""` (`''.codePointAt(0)` is `undefined`). Characterized (not fixed) by Notidian-8fwj; the assertion `"THROWS TypeError on empty string"` is **locked characterization** at `stickers.test.ts:200` and must be deliberately re-blessed (same posture as ADR 0025 / 0030 / 0033). **LOW present-risk:** `nativeToUnified` has **zero non-test callers** (verified) — this hardens the codec contract for future/external callers, not a live crash. Decisive grounding: the production-facing wrapper `emojiFromString("")` **already returns `""`** (pinned `stickers.test.ts:114`), so the family's production face already has an empty-in/empty-out contract. Recommends **Option A** — guard `nativeToUnified` to return `""` (`native.codePointAt(0)?.toString(16) ?? ""`) and flip the locked assertion — making the codec pair **total**, keeping the return type `string` (no caller null-check), and matching the existing `emojiFromString` contract — over **B** (document as caller precondition + guard at call sites: pushes the same guard onto every future caller, leaves the pair non-total) or **C** (decline: leaves a latent crash). Same-family note: `unifiedToNative` throws `RangeError` on empty/non-hex/out-of-range (pinned `stickers.test.ts:80-98`); recommends a **narrow** symmetric empty-only guard (`unifiedToNative('') -> ''`) but **keeping** the non-hex/out-of-range `RangeError` loud — it is load-bearing for `emojiFromString`'s catch (the Notidian-ebz security verbatim-return contract). Pure offline-provable string logic; gate is jest, no flag/eyes-on. Awaiting owner direction (bd Notidian-ywcf). |
| [0043](0043-stringcompare-non-string-operand-text-matcher-semantics.md) | TEXT-matcher semantics for a non-string non-nullish operand (`stringCompare` `.toLowerCase()` TypeError on number `0` / boolean `false`) (**Proposed**) | Open behavior decision on the table-view filter path. `stringCompare` (`filter.ts:63-70`), the matcher behind `include`/`notInclude`, guards operands with only `(value ?? "")` — which catches null/undefined but **not** a non-string non-nullish primitive — so a flex/number cell whose raw JSON value is `0` (or a `boolean false`) reaches `.toLowerCase()` on a non-string and throws `TypeError`, and `filterReturnForCol` (`filter.ts:140-157`) has **no try/catch**, so it crashes the **entire** table-view filter pass (live, via `linkContextRow`/`treeHelpers`/`ContextEditorContext`). Characterized (not fixed) by Notidian-u8yx; the `toThrow(TypeError)` **DEFECT-PIN** at `filterFnTypes.test.ts:185-188` and `:373-394` must be deliberately re-blessed (same posture as ADR 0025/0030/0032/0033/0042). Verified the shared gap has two modes: `stringCompare`/`startsWith`/`endsWith` **throw** (String-only method on a non-string), but `startsWith`/`endsWith`/`lengthEquals` are **not dispatched** (latent), and `lengthEquals`/`empty` fail closed *by accident* (`(0).length` is `undefined` -> comparison false). Recommends **Option A** — FAIL-CLOSED-EMPTY (`typeof v === "string" ? v : ""`) applied **uniformly** to the shared non-string gap across `stringCompare`/`startsWith`/`endsWith`/`empty`/`lengthEquals`, and flip the locked DEFECT-PIN — matching the established value-level fail-closed convention (lessThan/greaterThan/lengthEquals/date: a non-matching operand never spuriously matches, never throws; same posture as ADR 0032(b), defensibly opposite to ADR 0034's operator-level fail-open) — over **B** (`String(value ?? "")` coerce-to-string: silently makes `0` substring-match `"0"`, `false` match `"false"` — a footgun behavior change with no user signal) and **C** (route-by-type at the dispatch layer: most correct long-term but largest blast radius, and the flex ambiguity re-creates the question one layer up — recorded as the eventual direction). Pure offline-provable predicate logic; gate is jest, no flag/eyes-on. Awaiting owner direction (bd Notidian-9i9i). |
| [0044](0044-context-insert-row-create-authority-gate.md) | `api.context.insert` row-create — apply a per-field authority gate? (**Proposed**) | Open authority-partition decision: `api.context.insert`'s default-`'files'`-schema branch (`api.ts:307-334`) writes the **whole** input row (minus `PathPropertyName`) to the new file's frontmatter via `saveProperties` with **no per-field gate**, while `api.context.update`/`api.path.setProperty` route each field through `apiFieldWriteTarget` (ADR 0001/0017, Notidian-1da). The no-gate behavior is **pinned as characterization** in `api.authority.context.test.ts:196-226`. Genuinely two-sided: on a *create* there is no prior value to leak from (seeding the visible file is arguably legitimate), but the durable-home partition (ADR 0001/0014/0017) does not depend on *when* the write happens. Recommends **Option B** — FULL GATE: route each insert field through `apiFieldWriteTarget` so a `source:"notidian"` / context-only field lands in the context MDB (its only durable home) and a computed/read-only field is skipped, while ordinary frontmatter fields still seed the new file's YAML — making `insert` consistent with the already-gated `update`/`setProperty` verb it should mirror (seeding an MDB-owned column to file YAML re-introduces the frontmatter-vs-MDB split the gate prevents; seeding a computed value persists a derived snapshot). Over **A** (no gate, status quo — leaves `insert` the one un-gated value-write verb, an authority hole that grows as more columns declare `source:"notidian"`) or **C** (partial gate — skip computed only, still seed `source:"notidian"` to frontmatter: a half-measure that still seeds the MDB-owned field, the main split B prevents, and partitions fields a third inconsistent way). Cost: one deliberate characterization re-bless. Pure offline-provable routing logic; gate is jest, no flag/eyes-on. Awaiting owner direction (bd Notidian-2yh). |
| [0045](0045-replacedb-create-replace-column-alignment.md) | How should `replaceDB` align the REPLACE row VALUES with the CREATE TABLE column list? (**Proposed**) | Open correctness-cleanup decision in the whole-table-rewrite SQL builder. `replaceDB` (`src/adapters/mdb/db/db.ts:433-475`) builds the CREATE column list from `uniq(cols).filter(f=>f)` (de-duped + falsy-dropped) but maps each row's `REPLACE INTO ... VALUES` over the **full, un-deduped** `cols` — so `cols=['a','a','','b']` yields a 2-column CREATE `("a","b")` fed a 4-value REPLACE. **Real-engine ground truth** (`db.realengine.roundtrip.test.ts:552-614`, sql.js 1.8.0): DIRECT it **throws** `table t has 2 columns but 4 values were supplied`; via `replaceDB` the throw is **swallowed** → returns `false`, `selectDB`→`null` (row silently **dropped**), the table survives. **Latent today** because production `cols` come from `mdbTablesToDBTables` (`db.ts:293-305`, `field.name` projection — normally unique/non-empty); the `CONTRAST` case proves clean round-trips. Pinned as characterization in **two** nets — the builder net (`db.sql-builders.test.ts:363-387`) and the engine net — both must be deliberately re-blessed. Recommends **Option A** — emit an **explicit column list** `REPLACE INTO t ("a","b") VALUES (...)` from the **same** `uniq+filter` list as CREATE: correct **by construction** for any dup/empty/**reordered** `cols`, the only option robust to both dedup AND positional drift, and it makes the dup/empty case **store the row** instead of silently dropping the save. Over **B** (map VALUES over the same `uniq+filter` list, no column list — fixes the count but stays **positional**, mis-maps silently on column-order drift) or **C** (keep + document — leaves a latent silent-write-loss footgun gated by an implicit, unenforced upstream invariant). `insertIntoDB` is out of scope (positional, owns no CREATE). Pure offline-provable SQL-string construction; gate is jest, no flag/eyes-on. Awaiting owner direction (bd Notidian-k778). |
| [0046](0046-sql-batch-statement-seam-cleanup.md) | Should the `insertIntoDB`/`updateDB` batched-statement seam (leading space + `;;  ` double-semicolon) be cleaned up? (**Proposed**) | Open **cosmetic** cleanup decision on the SQL-builder file (sibling of ADR 0045, orthogonal concern). `insertIntoDB` (`db.ts:357-363`) and `updateDB` (`db.ts:383-389`) build each table's `rowsQuery` with a `reduce` **seeded with `""`** and a `${prev} ` template, so every statement carries a **leading space**; each per-table `rowsQuery` also ends in `;`, then `serializeSQLStatements` (`serializers.ts:4`) joins with `'; '` — so the two-table seam is `;` + `; ` + leading-space = **`;;  `** (double semicolon + two spaces). **Benign:** an empty statement is a SQLite no-op (proven harmless end-to-end by the real-engine net `db.realengine.roundtrip.test.ts`, `Notidian-0jtp`), idents stay quoted, values stay single-quote-doubled — **zero security/correctness impact**. Pinned verbatim in ~11 `db.sql-builders.test.ts` cases (the `;;  ` seam at `:300-317`). `replaceDB` does **not** have this seam (it `.map()`+`push`es and execs one-at-a-time, no seed-reduce, no join). Recommends **Option C if `Notidian-k778` (ADR 0045) is accepted** — fold the array+`.join('; ')` cleanup into the same SQL-builder pass so the ~11 pinned assertions are re-blessed in **one** reviewed round alongside k778's flip (cheaper than two) — **else Option A** (leave as-is: zero functional value, so don't spend an isolated assertion-flip review on aesthetics; the pins already document it as intentional). **Ruled out: B** standalone (the fix is small/safe but spends a re-bless of ~11 pins on zero functional value — review-debt churn for aesthetics; = C-without-a-carrier). Pure offline-provable SQL-string construction; gate is jest, no flag/eyes-on. Awaiting owner direction (bd Notidian-p5qt). |
| [0047](0047-db-nul-transport-limitation.md) | `db.exec(string)` cannot transport a NUL (0x00) in a value — bind-param API vs NUL-stripping vs accept+document (**Proposed**) | Open **transport/API-limitation** decision on the SQL-builder file (transport sibling of ADR 0045/0046, distinct from the escaping family). `sql.js` `db.exec`/`db.run` hand SQL **text** to the WASM C engine as a NUL-terminated **C string**, so a `0x00` in a value (spliced into the SQL text at `db.ts:361/385/453`) **truncates the statement at the NUL**; the engine throws, `replaceDB`'s try/catch swallows it and returns **`false`**, silently **losing the whole table's rows for that save**. **Real-engine ground truth** (`db.realengine.roundtrip.test.ts:500-528`, sql.js 1.8.0): DIRECT `db.exec` of a NUL-bearing literal **throws**; via `replaceDB` it **swallows → `false`**, stores nothing. **NOT a `quoteIdent`/`sanitizeSQLStatement` escaping defect** — the same net proves **every** byte but NUL (incl. the whole `0x01`-`0x1f` C0 range) round-trips byte-for-byte; there is **no NUL-safe escape** for a C-string transport. **Latent** (row values come from frontmatter/markdown, where a literal NUL is rare). Recommends **Option B (interim)** — strip **just** NUL in `sanitizeSQLStatement` (`/\x00/g`, the one chokepoint every value write passes through): smallest blast radius, removes the whole-table silent-loss footgun at near-zero cost — **paired with a `bd remember` that Option A is the eventual correct fix** — over **A** (move row-value writes to the parameter-bound API `db.prepare`+`bind` / `db.run(sql, params)`, already exposed by the pinned engine at `sql-wasm.js:256/458/514` — the **byte-faithful, architecturally correct** fix, but a **larger cross-cutting seam change** across all builders + transaction-shape rework, unjustified *now* for a latent case; sequence it as a scoped refactor, optionally folding ADR 0045/0046) or **C** (accept+document — leaves a single anomalous byte able to silently roll back an entire table's save, no signal: rejected as the resting state). Pairs with ADR 0039/`Notidian-jlb5` (guard a NUL at *source*) as defence-in-depth. Pure offline-provable; gate is jest, no flag/eyes-on. Awaiting owner direction (bd Notidian-dgo6). |
| [0048](0048-resolvepath-parent-overpop-root-clamp.md) | `resolvePath('../…')` over-pop past root — explicit root clamp vs keep+document the graceful degradation (**Proposed**) | Open **behavior-contract** decision on the pure `resolvePath` identity primitive (sibling of the `'./'`-resolution fix `Notidian-2i5k` on the same file). The `'../'` branch (`path.ts:31-40`) runs `while(pathParts[0]==='..') sourceParts.pop()` with **no clamp**, so when `..` count exceeds source depth it `pop()`s an **empty** array (`undefined`, no throw): `resolvePath('../../../a.md','A/B.md')` → **`'a.md'`** (rootless), `resolvePath('../../','A/B.md')` → **`''`**. **Property-locked** that it **never** emits a leading `/` or `//` — graceful, just not a *defined* clamp. **Sharpened POSIX framing (load-bearing):** `path.posix.resolve('/A','../../../a.md')==='/a.md'` (**clamps**) but `path.posix.normalize('A/../../../a.md')==='../../a.md'` (**keeps** `..`); under the no-leading-slash invariant (`Notidian-2i5k`), `path.resolve`'s `/a.md` minus its leading slash **is exactly `a.md`** — so the current over-pop output **already equals** `path.resolve`'s clamp for the common case (accidental clamp, not `..`-preserving). Thus A and the current behavior **return the same string**; on the table is **defined contract+guard (A)** vs **emergent accident named as contract (B)**. **Present-risk LOW:** prod routes via `spaceManager.resolvePath`'s `if(resolvedPath!==path) return resolvedPath` — an over-pop changes the string, so the bare-leaf is returned and *matched* against `pathsIndex`, staying a **non-matching key** (the dangling-link contract `relationResolver.ts` was built around); the `''`-collapse flows as a benign no-link key. Over-pop **requires a malformed link** to arise and does **not** re-point to a different real row. Recommends **Option B (keep+document)** — **no code change**; name today's graceful behavior as the contract in the `path.ts` comment + locked test comments: zero behavior change to a ~56-caller identity primitive (ADR 0014/0016), **no caller shown harmed**, **no locked assertion flipped** (no re-bless debt), and it avoids A's **asymmetric risk** (a malformed link deterministically re-pointing to a real root `a.md`). Over **A** — add `if(sourceParts.length>0) sourceParts.pop();` to make the clamp explicit (matches `path.resolve`, removes the `pop()`-on-`[]` reader-trap, low diff, bare-leaf values unchanged): real-but-low-value semantic change for a malformed-input-gated edge, a clean readability-only follow-up but lower value than naming the existing contract. Ruled out **C** (clamp behind a default-OFF flag) — over-engineers a pure offline deterministic string question; both outcomes jest-provable + near-identical, flag adds settings+branch+doubled matrix for zero payoff (no-flag posture of ADR 0032/0033/0034/0036/0039/0042). Also ruled out a `normalize`-style "preserve `..`" contract (emits a leading `..`, no valid vault key, vault has no parent of root). LOCKED characterization at `resolvePath.test.ts:264-289` + the leading-slash property at `:355-365` must be re-blessed only on a pick. Pure offline-provable; gate is jest, no flag/eyes-on. Awaiting owner direction (bd Notidian-ircw). |
| [0049](0049-remove-dormant-quick-find.md) | Remove the dormant quick-find apparatus — **supersedes [ADR 0041](0041-consolidate-view-search-affordances.md)'s keep-dormant sub-decision** (**Accepted**, executed) | Executed cleanup at the owner's explicit request ("remove the redundant find affordance"). ADR 0041 consolidated to one view search but **kept** the highlight-on-match quick-find engine (`QuickFindBar.tsx`, `tableQuickFind.ts`, `findOpen` state, `mk-cell-find-*` highlight + reveal math) **dormant** for a possible A1 "search mode" follow-up. Grep verified (before *and* after) that `setFindOpen(true)` had **zero callers** — the apparatus was **fully unreachable dead code**, and the literal **"Find in view"** placeholder the owner objected to still lived in `QuickFindBar`. **Deleted outright:** `QuickFindBar.tsx`, `tableQuickFind.ts` + its test, all `findOpen`/`setFindOpen` members on `ContextEditorContext`, every quick-find state/memo/effect/render/cell-highlight branch in `TableView.tsx`, the `.mk-quick-find*` / `.mk-td.mk-cell-find-*` CSS, and the stale `setFindOpen` test stubs. **Kept (live search, unregressed):** `SearchBar` + focus-on-mount, the `mk-view-search-toggle` magnifier, `searchActive`/`setSearchActive`, and the `Cmd/Ctrl+F` → `setSearchActive(true)` path; the `FilterBar.anchor` `.mk-quick-find-toggle`-is-null assertion stays valid. View-layer only — no authority/sink change (no `innerHTML` sink existed; cell-class highlight). **Executed decision, not a decision-that-waits:** owner made the product call, change is tsc/jest/build-provable, shipped same session. (Requested as "ADR 0042" but that number was taken → allocated **0049**.) bd `Notidian-fws1`. |

## Historical Records

These records are kept because they explain why the architecture changed. They do not define the active roadmap unless a future ADR explicitly reactivates them.

| ADR | Status | Why keep it |
| --- | --- | --- |
| [0004](0004-authority-hardening-transactions-and-reconciliation.md) | Historical hardening context | Records the phase that introduced the authority registry, frontmatter write gating, conservative type reconciliation, and rename reconciliation. Later focused ADRs own the active rules. |
| [0005](0005-obsidian-bases-alignment-without-replacing-contexts.md) | Superseded by ADR 0014 | Preserves the Bases authority-model lessons that informed Notidian's current source-of-truth model. |
| [0011](0011-bases-first-convergence.md) | Superseded by ADRs 0013 and 0014 | Records why Bases-first convergence was explored before the personal-tool direction changed. |
| [0012](0012-custom-bases-view-feasibility-gate.md) | Retired by ADR 0014 | Records the custom Bases view experiment and why it was removed from the active architecture. |
| [0013](0013-notidian-first-canonical-file-architecture.md) | Superseded by ADR 0014 | Records the intermediate Notidian-first/Bases-compatible strategy and why Bases compatibility was later dropped as a target. |

## Decision Summary

Notidian uses a Notidian-only personal database architecture:

- File paths and file names are canonical page identity.
- Markdown frontmatter is canonical ordinary note metadata.
- Notidian is the only intended database engine/interface.
- Notidian context MDB files store view configuration, ordering, formulas, relations, display schema, legacy state, and explicitly Notidian-owned fields.
- Native Bases and `.base` compatibility are not part of the active architecture.
- Projected values from files/frontmatter may be cached for rendering, but they must be rebuilt from the owning layer and must not become the durable source of truth.
- Property create/rename/delete operations must be planned against canonical frontmatter before any destructive write is offered.

## Maintenance Rule

Changes that only add implemented behavior inside the accepted authority model should update [Current State](../current-state.md). Add or update an ADR only when a durable architectural decision changes.

Update historical ADRs only for correction or status clarification. Do not treat historical ADRs as active roadmap items.
